{
  "name": "checkpoint-bench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "本地流处理检查点实验台：分区事件、聚合处理器、原子检查点、故障注入与版本迁移",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "~5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.9"
  }
}
